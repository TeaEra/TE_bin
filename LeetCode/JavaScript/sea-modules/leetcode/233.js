define(function (require, exports, module) {

    /**
     * Number of Digit One
     */
    
    'use strict';

    /**
     * @param {number} n
     * @return {number}
     */
    var countDigitOne = function(n) {
        /*if (n <= 0) {
            return 0;
        }
        var i = 1,
            res = 0;
        for (i=1; i<=n; ++i) {
            var tmp = String(i).match(/1/g);
            res += tmp !== null ? tmp.length : 0;
        }
        return res;*/

        //
    };

    module.exports = countDigitOne;

});