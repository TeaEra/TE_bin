/**
 * Summary Ranges
 */

'use strict';

/**
 * @param {number[]} nums
 * @return {string[]}
 */
var summaryRanges = function(nums) {
	var 
		size = nums.length,
		outArr = [];
	if (size === 0) {
		return outArr;
	}
	// TODO: !!!
};