/**
 * @param {string[]} cpdomains
 * @return {string[]}
 */
var subdomainVisits = function (cpdomains) {
  let cntMapper = {};
  cpdomains.forEach(cpdomain => {
    let [cnt, domain] = cpdomain.split(' ');
    // domain.split('.').forEach(()); // TODO:
  });
};
